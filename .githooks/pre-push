#!/bin/sh

# Fast local gate: build + lint + test only.
# Full checks (E2E, guards) run in CI.
pnpm build && pnpm lint && pnpm test
status=$?
if [ $status -ne 0 ]; then
  echo "pre-push blocked: build/lint/test failed"
  exit $status
fi
