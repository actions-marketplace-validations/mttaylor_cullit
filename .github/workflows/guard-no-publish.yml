name: Package Publish Guard Notice

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish-guard-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Print guard context
        run: |
          echo "Cullit is now managed as a single open-source repository."
          echo "This workflow is informational and does not block releases."
